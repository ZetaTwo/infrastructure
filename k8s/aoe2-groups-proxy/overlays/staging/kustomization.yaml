apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: aoe2-groups-proxy-staging
resources:
  - ../../base
  - namespace.yaml
  - ingress.yaml
patches:
  - path: allowed-origins-patch.yaml
images:
  # CI's deploy-staging job rewrites this on every push to main. Keep it
  # the only `newTag:` in this file.
  - name: ghcr.io/zetatwo/aoe2-groups-proxy
    newTag: 3459f0d701024690f7f96b6c76ac3de34527f812
