apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: aoe2-groups-proxy-staging
resources:
  - ../../base
  - namespace.yaml
  - ingress.yaml
patches:
  - path: allowed-origins-patch.yaml
images:
  # CI's deploy-staging job (aoe2-streaming's backend-deploy.yml) rewrites
  # this line on every push to main. Keep it as the only `newTag:`
  # occurrence in this file.
  - name: ghcr.io/zetatwo/aoe2-groups-proxy
    newTag: db56d1e19304b54f90a6726a0c6b87148206b716
