apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: aoe2-groups-proxy
resources:
  - ../../base
  - namespace.yaml
  - ingress.yaml
images:
  # CI's deploy-production job rewrites this on release only. Keep it the
  # only `newTag:` in this file.
  - name: ghcr.io/zetatwo/aoe2-groups-proxy
    newTag: v1
